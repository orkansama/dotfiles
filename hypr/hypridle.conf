general {
    lock_cmd = pidof hyprlock || hyprlock
    after_sleep_cmd = hyprctl dispatch dpms on
}

listener {
    timeout = 300
    # timeout = 5 # Test-Time
    on-timeout = ~/.config/hypr/scripts/screensaver-startup.sh
    on-resume = ~/.config/hypr/scripts/screensaver-kill.sh
}
